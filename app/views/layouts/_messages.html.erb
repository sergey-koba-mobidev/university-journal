<script>
    $(document).ready(function () {
    <% flash.each do |name, msg| %>
        <% if msg.is_a?(String) %>
        Materialize.toast('<%= msg %>', 4000, '<%= name.to_s == 'notice' ? 'teal lighten-2' : 'red darken-2' %>');
        <% end %>
    <% end %>
    });
</script>