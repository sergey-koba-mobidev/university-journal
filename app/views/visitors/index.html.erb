<h3><%= t :welcome %></h3>
<% if(user_signed_in?) %>
    <% if !current_user.student? %>
        <%= render 'teacher' %>
    <% else %>
        <%= render 'student' %>
    <% end %>
<% else %>
    <%= t :please %> <%= link_to t(:sign_in), new_user_session_path %> to view your Journal.
<% end %>