<div class="row">
<%= form_for(@discipline, html: {class: 'form-horizontal col-md-3 col-md-offset-1'}) do |f| %>
  <% if @discipline.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@discipline.errors.count, "error") %> prohibited this discipline from being saved:</h2>

      <ul>
      <% @discipline.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
      <div class="row">
        <div class="input-field col s12 m6">
          <%= f.text_field :title, :autofocus => true %>
          <%= f.label t(:title) %>
        </div>
        <div class="input-field col s12 m6">
          <% if current_user.admin? %>
            <%= f.collection_select(:user_id, User.teacher, :id, :name) if current_user.admin? %>
            <%= f.label t(:teacher) %>
          <% end %>
        </div>
      </div>

      <%= button_tag t(:save), class: 'btn waves-effect waves-light', type: 'submit' %>
<% end %>
</div>